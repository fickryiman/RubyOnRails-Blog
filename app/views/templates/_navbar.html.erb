<div class="nav">
  <input type="checkbox" id="nav-check">
  <div class="nav-header">
    <div class="nav-title" href="<% root_path %>">
      Ruby on Rails Blog App
    </div>
  </div>
  <div class="nav-btn">
    <label for="nav-check">
      <span></span>
      <span></span>
      <span></span>
    </label>
  </div>
  
  <div class="nav-links">
    <% if user_signed_in? %>
      <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "link-nav" %>
    <% else %>
      <%= link_to "Login", new_user_session_path, method: :delete %>
    <% end %>

    <% if !user_signed_in? %>
      <%= link_to "Register", new_user_registration_path, method: :get %>
    <% end %>
  </div>
</div>